[package]
name = "engine"
version = "0.1.0"
authors = ["Trikzon <trikzondev@gmail.com>"]
edition = "2018"

[lib]
name = "engine"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]

[target.'cfg(target_os = "android")'.dependencies]
jni = { version = "0.17", default-features = false }
